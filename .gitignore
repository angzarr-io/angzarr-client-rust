# Build artifacts
target/
Cargo.lock
*.rs.bk

# cargo-mutants
mutants.out/
mutants.out.old/
# Ephemeral mutation build cache (compiled artifacts only, never mutated source)
.mutants-cache/

# Generated Rust code — emitted by `just generate-proto`. Never committed.
# See project_proto_generation_model.md.
src/proto/*.rs
!src/proto/.gitkeep

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# ctxloom ephemeral data (synced bundles, session data, context files)
.ctxloom/ephemeral/
